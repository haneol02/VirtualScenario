# VirtualScenario - 테스트 가이드

## 🎯 테스트 준비

### 서버 실행
```bash
# 터미널 1: Backend
cd backend
npm run dev
# ✅ Server running on http://localhost:3001

# 터미널 2: Frontend
cd frontend
npm run dev
# ✅ Local: http://localhost:3000
```

### 브라우저 접속
- URL: http://localhost:3000
- 권장 브라우저: Chrome, Edge (WebGL 지원)

---

## ✅ 테스트 시나리오

### **1. 프로젝트 생성 및 관리**

#### 1-1. 새 프로젝트 만들기
1. Dashboard → "새 프로젝트" 버튼 클릭
2. 제목: "안전교육 시나리오 1"
3. 설명: "코레일 승강장 안전교육"
4. 버전: "1.0"
5. "생성" 클릭
6. **기대 결과**: 프로젝트 카드 생성됨

#### 1-2. 프로젝트 편집
1. 프로젝트 카드 → "✏️ 편집" 클릭
2. SceneEditor 화면 로드 확인

---

### **2. 씬 생성 및 관리**

#### 2-1. 씬 추가
1. SceneEditor → 좌측 "🎬 씬" 탭
2. "+ 추가" 버튼 클릭
3. 씬 제목: "장면 1: 승강장 도착"
4. **기대 결과**: 씬 목록에 추가됨

#### 2-2. 씬 선택
1. 씬 카드 클릭
2. **기대 결과**:
   - 씬 카드 파란색으로 하이라이트
   - 3D 뷰어 활성화
   - 우측 패널 표시

---

### **3. 배경 맵 설정**

#### 3-1. 배경 맵 선택
1. 좌측 "🎨 배경" 탭 클릭
2. 배경 맵 선택 (예: "승강장")
3. **기대 결과**:
   - 3D 뷰어에 배경 오브젝트 표시
   - 배경 맵 카드에 "적용됨" 표시

#### 3-2. 배경 맵 만들기
1. "배경 맵 만들기 →" 클릭
2. BackgroundMapEditor로 이동
3. "새 배경 맵 만들기" 클릭
4. 이름: "선로 구간"
5. 설명: "기찻길 구간"
6. 아이콘: "🛤️"
7. **기대 결과**: 배경 맵 생성 및 편집 모드 진입

---

### **4. 3D 에셋 라이브러리** ⭐ NEW!

#### 4-1. 에셋 라이브러리 열기
1. SceneEditor → 좌측 "📦 에셋" 탭 클릭
2. **기대 결과**:
   - 에셋 목록 표시
   - 카테고리 필터 버튼 (전체, primitive, model, person, train, facility, sign)
   - "+ 3D 모델 업로드" 버튼 표시

#### 4-2. 3D 모델 업로드 (GLB/GLTF 권장)
1. "+ 3D 모델 업로드" 클릭
2. 파일 선택: [테스트용 GLB 파일]
   - 예시: https://github.com/KhronosGroup/glTF-Sample-Models 에서 다운로드
   - 추천: `BoxTextured.glb`, `Duck.glb`
3. 에셋 이름: "테스트 모델"
4. 카테고리: "model"
5. "업로드" 클릭
6. **기대 결과**:
   - 업로드 진행 상태 표시
   - "업로드 성공!" 알림
   - 에셋 목록에 추가됨 (GLB 태그 표시)

#### 4-3. 에셋 필터링
1. 카테고리 버튼 클릭 (primitive, model 등)
2. **기대 결과**: 해당 카테고리 에셋만 표시

#### 4-4. 에셋을 씬에 추가
1. 씬 선택 (좌측 "🎬 씬" 탭)
2. "📦 에셋" 탭으로 전환
3. 에셋 카드 클릭 (예: "테스트 모델" 또는 프리미티브 "사각형")
4. **기대 결과**:
   - 자동으로 씬에 오브젝트 생성됨
   - 3D 뷰어에 오브젝트 표시
   - 우측 "오브젝트" 패널에 추가됨

---

### **5. 3D 오브젝트 편집**

#### 5-1. 프리미티브 오브젝트 추가
1. "📦 에셋" 탭 → "primitive" 카테고리
2. "사각형" 클릭
3. **기대 결과**: 3D 뷰어에 Box 렌더링

#### 5-2. 3D 모델 오브젝트 추가
1. "📦 에셋" 탭 → "model" 카테고리
2. 업로드한 3D 모델 클릭
3. **기대 결과**: 3D 뷰어에 GLB/OBJ/FBX 모델 렌더링

#### 5-3. Transform 조작
1. 3D 뷰어에서 오브젝트 클릭
2. **TransformControls (Gizmo) 테스트**:
   - **이동 (Translate)**: 화살표 드래그
   - **회전 (Rotate)**: 원형 핸들 드래그
   - **스케일 (Scale)**: 정육면체 핸들 드래그
3. **기대 결과**:
   - 실시간 변형
   - 우측 패널에 숫자 업데이트

#### 5-4. 네임태그 표시
1. 오브젝트 선택
2. 우측 패널 → "네임태그 표시" 체크박스 ON
3. **기대 결과**: 3D 뷰어에 오브젝트 이름 표시

#### 5-5. 색상 변경
1. 오브젝트 선택
2. 우측 패널 → Color Picker 클릭
3. 색상 선택 (예: 빨강 #FF0000)
4. **기대 결과**:
   - 프리미티브: 색상 즉시 변경
   - 3D 모델: 모델 전체 색상 적용

---

### **6. Path 애니메이션 (타임라인 UI)** ⭐ NEW!

#### 6-1. 타임라인 패널 확인
1. 씬에 오브젝트 추가
2. **기대 결과**: 화면 하단에 Adobe Animate 스타일 타임라인 표시
   - 좌측: 오브젝트 레이어 리스트
   - 중앙: 타임 눈금자 (0초 ~ 최대 시간)
   - 우측: 줌 컨트롤 (25%, 50%, 100%, 200%)
   - 하단: 재생/일시정지 버튼

#### 6-2. 키프레임 추가
1. 오브젝트를 특정 위치로 이동
2. 타임라인에서 특정 시간 클릭 (예: 5초)
3. "K" 키 누르기 또는 타임라인 우클릭 → "키프레임 추가"
4. **기대 결과**: 타임라인에 다이아몬드 키프레임 마커 생성

#### 6-3. 키프레임 삭제
1. 키프레임 마커 우클릭
2. "키프레임 삭제" 선택
3. **기대 결과**: 키프레임 제거됨

#### 6-4. 타임라인 스크러빙
1. 플레이헤드 드래그
2. **기대 결과**:
   - currentTime 업데이트
   - 오브젝트 위치 실시간 반영 (향후 구현)

---

### **7. Undo/Redo 시스템**

#### 7-1. Undo 테스트
1. 오브젝트 Transform 변경
2. **Ctrl+Z** (Mac: Cmd+Z) 누르기
3. **기대 결과**: 이전 상태로 복원

#### 7-2. Redo 테스트
1. Undo 후 **Ctrl+Shift+Z** 누르기
2. **기대 결과**: 다시 실행

#### 7-3. UI 버튼 테스트
1. 좌측 상단 "↶ / ↷" 버튼 클릭
2. **기대 결과**:
   - 가능할 때: 파란색, 클릭 가능
   - 불가능할 때: 회색, 비활성화

---

### **8. 시뮬레이터 재생**

#### 8-1. 시뮬레이터 실행
1. Dashboard → 프로젝트 카드 → "▶️ 재생" 버튼
2. **기대 결과**: Simulator 화면 전환

#### 8-2. 재생 컨트롤
1. **▶️ 재생**: 시뮬레이션 시작
2. **⏸️ 일시정지**: 재생 중단
3. **⏹️ 정지**: 첫 씬으로 이동
4. **⏮️ 이전 / ⏭️ 다음**: 씬 네비게이션
5. **타임라인 슬라이더**: 드래그로 시간 이동
6. **속도 조절**: 0.5x, 1x, 2x

#### 8-3. 자막 표시
1. 대화가 있는 씬 재생
2. **기대 결과**:
   - 화면 하단에 자막 Fade-in
   - 발화자 이름 표시
   - 시간에 맞춰 자동 표시/숨김

---

## 🐛 예상 이슈 및 해결방법

### Issue 1: 3D 모델이 보이지 않음
**원인**:
- 파일 경로 문제
- 모델 크기가 너무 작거나 큼
- 카메라 위치 문제

**해결**:
1. 브라우저 개발자 도구 (F12) → Console 확인
2. 에러 메시지 확인 (404, CORS 등)
3. 카메라 줌아웃 (마우스 휠)
4. 오브젝트 Scale 조정 (10배로 키우기)

### Issue 2: 업로드 실패
**원인**:
- 파일 크기 초과 (50MB 제한)
- 지원하지 않는 형식

**해결**:
1. 파일 크기 확인
2. 지원 형식: .glb, .gltf, .obj, .fbx
3. Backend 로그 확인 (Terminal 1)

### Issue 3: 타임라인이 표시되지 않음
**원인**: 씬에 오브젝트가 없음

**해결**:
1. 오브젝트를 먼저 추가하세요
2. 오브젝트 추가 후 자동으로 타임라인 표시

---

## 📊 테스트 체크리스트

### Phase 1-2: 기본 기능
- [ ] 프로젝트 CRUD
- [ ] 씬 CRUD
- [ ] 배경 맵 선택
- [ ] 3D 오브젝트 추가 (프리미티브)
- [ ] Transform Controls
- [ ] 네임태그 표시
- [ ] Undo/Redo
- [ ] 대화/자막 추가
- [ ] 시뮬레이터 재생

### Phase 3: 고급 기능 ⭐
- [ ] **에셋 라이브러리 UI** ✅
- [ ] **3D 모델 업로드 (GLB)** ✅
- [ ] **3D 모델 업로드 (OBJ)** ✅
- [ ] **3D 모델 업로드 (FBX)** ✅
- [ ] **3D 모델 씬에 추가** ✅
- [ ] **3D 모델 렌더링** ✅
- [ ] **3D 모델 색상 변경** ✅
- [ ] **타임라인 UI** ✅
- [ ] **키프레임 추가/삭제** ✅
- [ ] Path 애니메이션 재생 (구현 예정)

---

## 🎥 테스트용 3D 모델 다운로드

### GLB/GLTF (권장)
- **Khronos glTF 샘플**: https://github.com/KhronosGroup/glTF-Sample-Models/tree/master/2.0
  - `BoxTextured.glb` (간단)
  - `Duck.glb` (애니메이션)
  - `Avocado.glb` (텍스처)

### OBJ
- **Free3D**: https://free3d.com/3d-models/obj
- **CGTrader**: https://www.cgtrader.com/free-3d-models/obj

### FBX
- **Adobe Mixamo**: https://www.mixamo.com (캐릭터 모델)
- **Sketchfab**: https://sketchfab.com

---

## 🔍 디버깅 팁

### 브라우저 개발자 도구
```
F12 → Console 탭
- API 에러 확인
- Three.js 로딩 에러 확인
- CORS 에러 확인
```

### Backend 로그 확인
```bash
cd backend
npm run dev
# 로그 출력 확인
```

### 네트워크 요청 확인
```
F12 → Network 탭
- API 요청 상태 (200, 404, 500)
- 파일 업로드 상태
- Static 파일 로딩
```

---

마지막 업데이트: 2025-11-08 19:00
